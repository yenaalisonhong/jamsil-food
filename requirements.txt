# HTTP 클라이언트 (외부 장소 API 호출)
httpx>=0.27.0

# 환경 변수 관리 (.env 파일)
python-dotenv>=1.0.0

# 데이터 검증 및 직렬화
pydantic>=2.0.0
pydantic-settings>=2.0.0

# CLI 인터페이스
typer>=0.12.0
rich>=13.0.0

# 지리 거리 계산 (Haversine)
geopy>=2.4.0

# 개발/테스트
pytest>=8.0.0
